# Predictelligence Property — Environment Variables
# Copy to .env and fill in your values

# ── AI (enables Claude narrative + listing extraction) ──────────────────────────
ANTHROPIC_API_KEY=your_anthropic_key_here

# ── EPC Register (enables EPC area data enrichment) ────────────────────────────
# Free registration at https://epc.opendatacommunities.org/
# Format: email@example.com:your_api_key
EPC_API_KEY=your_email@example.com:your_epc_api_key_here

# ── Admin dashboard protection ──────────────────────────────────────────────────
# Set to protect GET /api/admin/stats with X-Admin-Key header
# Leave empty to allow unauthenticated access (dev only)
ADMIN_KEY=change-me-in-production

# ── Flask ───────────────────────────────────────────────────────────────────────
FLASK_ENV=development
PORT=5000

# ── Data paths (optional overrides) ────────────────────────────────────────────
# DATA_DIR=/path/to/custom/data/dir
# PPD_SQLITE_PATH=/path/to/ppd.sqlite
